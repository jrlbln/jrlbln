import { SiteShell } from "@/components/layout/site-shell";
import { HeroCard } from "@/components/panels/hero-card";

export default function Home() {
  return (
    <SiteShell>
      <HeroCard />
    </SiteShell>
  );
}
